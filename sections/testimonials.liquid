{% schema %}
{
  "name": "Testimonials",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Заголовок секции",
      "default": "Testimonials"
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "Отзывы клиентов",
      "default": "<p>Они лучшие в своем деле!</p>"
    }
  ],
  "presets": [
    {
      "name": "Testimonials"
    }
  ]
}
{% endschema %}

<section>
  <h2>{{ section.settings.heading }}</h2>
  <div>{{ section.settings.description }}</div>
</section>
